<div class="media profile-left">
    <a class="pull-left profile-thumb" href="profile.html">
        <img class="img-circle" src="{{ asset(app.user.image.uploadDir ~ '/' ~ app.user.image.id ~ '.' ~ app.user.image.url) }}" alt="profile">
    </a>
    <div class="media-body">
        <h4 class="media-heading">{{ app.user.username }}</h4>
        <small class="text-muted">Développeur</small>
    </div>
</div>